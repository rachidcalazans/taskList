<phone:PhoneApplicationPage 
    x:Class="TaskList.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <StackPanel>
        <TextBlock>Descrição:</TextBlock>
        <TextBox Name="txtDescricao"></TextBox>

        <TextBlock>Data de Início:</TextBlock>
        <toolkit:DatePicker x:Name="datInicio" />

        <TextBlock>Data de Término:</TextBlock>
        <toolkit:DatePicker x:Name="datTermino" />


        <ListBox Name="listaTasks">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0 20 0 0">
                            <TextBlock FontSize="20" FontWeight="Bold"
                                       Text="{Binding Description}"></TextBlock>
                        <TextBlock FontSize="10" FontWeight="Bold"
                                       Text="{Binding StartDate}" VerticalAlignment="Center"></TextBlock>
                        <TextBlock FontSize="10" FontWeight="Bold"
                                       Text="{Binding FinishDate}" VerticalAlignment="Center"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="btSalvar" Click="btSalvar_Click_1" Content="Salvar"/>
    </StackPanel>

</phone:PhoneApplicationPage>