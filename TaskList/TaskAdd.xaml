<phone:PhoneApplicationPage 
    x:Class="TaskList.TaskAdd"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Name="btBack" Width="104" Grid.Row="0" HorizontalAlignment="Left">Back</Button>
        <TextBlock Grid.Row="0" FontSize="50" HorizontalAlignment="Center">Add Task</TextBlock>
        <Button Name="btSave" Width="104" Grid.Row="0"  HorizontalAlignment="Right">Save</Button>
        
        <TextBlock Grid.Row="1" Grid.Column="0" FontSize="35" HorizontalAlignment="Left">Task Name </TextBlock>
        
        <TextBox Name="txtBusca" Grid.Row="2" Grid.Column="0" Width="350" HorizontalAlignment="Left"/>
        <Button Name="btAdd" Width="104" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right"> Add </Button>

        <TextBlock Grid.Row="3" FontSize="30" HorizontalAlignment="Left"> Lat.: Long.: </TextBlock>
        <Button Name="btPickLocation" Grid.Row="4" HorizontalAlignment="Right">Pick Location</Button>

        <TextBlock Grid.Row="5" FontSize="40" HorizontalAlignment="Center">Sub-Tasks</TextBlock>

        <ListBox Name="lstResultado" Grid.Row="6">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Margin="10" Text="{Binding description}" Grid.Row="0" Grid.Column="0"/>

                        <Button Name="btEdit" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right">Edit</Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Name="btDelete" 
                Grid.Row="7"
                HorizontalAlignment="Center">
            Delete Task
        </Button>

    </Grid>


</phone:PhoneApplicationPage>
